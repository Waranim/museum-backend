app:
  keycloak:
    auth-url: http://${KEYCLOAK_HOST:localhost}:${KEYCLOAK_PORT:8081}/realms/${KEYCLOAK_REALM:museum}/protocol/openid-connect
    admin-url: http://${KEYCLOAK_HOST:localhost}:${KEYCLOAK_PORT:8081}/admin/realms/${KEYCLOAK_REALM:museum}
    realm: ${KEYCLOAK_REALM:museum}
    client-id: museum-backend
    client-secret: ${KEYCLOAK_CLIENT_SECRET:vBFffuhSkZkREDDPcgKQ86KjMqSsuJkw}

springdoc:
  swagger-ui:
    path: /public/swagger-ui/index.html
  api-docs:
    path: /public/api-docs

#logging:
#  level:
#    org.springframework.security: trace